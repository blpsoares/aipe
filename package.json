{
  "name": "aipe",
  "type": "module",
  "private": true,
  "bin": {
    "aipe": "bin/aipe"
  },
  "scripts": {
    "build": "bun run scripts/build.ts",
    "build:host": "bun run scripts/build.ts host",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json",
    "version:check": "bun run scripts/version.ts"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.10.6",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/preact": "^3.2.4",
    "@types/bun": "latest",
    "happy-dom": "^20.10.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@preact/signals": "^2.9.3",
    "preact": "^10.29.7",
    "preact-iso": "^2.12.0",
    "yaml": "^2.9.0"
  }
}
